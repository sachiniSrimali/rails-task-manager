<h1> My Tasks </h1>
<ul>
  <% @tasks.each do |task| %>
    <li>  <input type="checkbox" <%= task.completed ? "checked" : '' %>> <%= link_to task.title, "/tasks/#{task[:id]}" %></li>
  <% end %>
</ul>
